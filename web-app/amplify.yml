version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist  # or build if using Create React App
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
